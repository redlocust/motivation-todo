var expect = require('expect');
var React = require('react');
var TodosApp = require('TodosApp');

describe('TodosApp', () => {
  it('should exist', () => {
      expect(TodosApp).toExist();
  });
});